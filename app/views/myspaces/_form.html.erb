<%= form_for(@myspace) do |f| %>
  <% if @myspace.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@myspace.errors.count, "error") %> prohibited this myspace from being saved:</h2>

      <ul>
      <% @myspace.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
